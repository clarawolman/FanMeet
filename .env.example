# URL del proyecto Supabase (Project Settings -> API)
VITE_SUPABASE_URL=

# anon key (Project Settings -> API). Es publica por diseno, se puede exponer
# en el frontend porque Supabase la protege con RLS.
VITE_SUPABASE_ANON_KEY=
